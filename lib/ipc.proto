syntax = "proto3";

import "google/protobuf/empty.proto";

package ipc;

option go_package = "ipc/protos;protos";

service Aikido {
  rpc OnReceiveDomain (Domain) returns (google.protobuf.Empty);
}

message Domain {
  string domain = 1;
}
