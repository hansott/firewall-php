syntax = "proto3";

import "google/protobuf/empty.proto";

package ipc;

option go_package = "ipc/protos;protos";

service Aikido {
  rpc OnReceiveDomain (Domain) returns (google.protobuf.Empty);
  rpc OnReceiveRequestMetadata (RequestMetadata) returns (google.protobuf.Empty);
}

message Domain {
  string domain = 1;
}

message RequestMetadata {
  string method = 1;
  string route = 2;
}