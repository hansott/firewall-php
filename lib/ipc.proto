syntax = "proto3";

package ipc;

option go_package = "ipc/protos;protos";

service Aikido {
  rpc OnReceiveDomain (Domain) returns (BoolResponse);
}

message Domain {
  string domain = 1;
}

message BoolResponse {
  bool success = 1;
}